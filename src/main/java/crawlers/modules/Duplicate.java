package crawlers.modules;

import java.util.List;

public class Duplicate {

	//Takes URLs and drop duplicate (seen before) URLs and returns never-seen-before-URLs
	public static void drop(List<String> urls) {
		
	}
}
